// function makeLogger(type) {
//   return (time, msg) => console.log(`${type} || ${time} || ${msg}`)
// }

// const warningLogger = makeLogger('WARNING');
// warningLogger('1:00', 'careful!!');

greeting(); 

function greeting () {
  console.log('hi');
};

